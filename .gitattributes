# Normalize line endings across platforms.
* text=auto eol=lf

# Windows-native scripts keep CRLF.
*.ps1 text eol=crlf
*.bat text eol=crlf

# Shell scripts must stay LF so the shebang works on Linux/macOS.
*.sh text eol=lf

# Binary assets.
*.pdf binary
*.png binary
*.jpg binary
