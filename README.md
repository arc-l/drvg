# Dynamic Rotation-Stacked Visibility Graph (DRVG)

> Repository: `git@github.com:arc-l/drvg.git`

This repository hosts both the code base for the **Dynamic Rotation-Stacked
Visibility Graph (DRVG)** and the associated paper draft.

## Overview

_Project description to be added._

## Repository Layout

| Path | Contents |
|------|----------|
| `code/` | DRVG implementation (to be added). |
| `latex/` | Paper draft (`main.tex`, `references.bib`) and pdflatex build scripts. |
| `build.sh` | Top-level Linux/macOS build entry point (`paper` / `code` / `all` / `clean`). |
| `docs/` | Project docs, including the chat input log. |

## Building

- **Paper (Windows):** `latex\compile.ps1`
- **Paper (Linux/macOS):** `bash latex/compile.sh` — or `bash build.sh paper`
- **Everything (Linux/macOS):** `bash build.sh all`

## Main Results

_Updated before each commit to summarize the main results of the current
version._

**Version `0000`:** Initial project scaffolding only — no algorithmic results
yet. Establishes the repository structure, the LaTeX paper skeleton, and the
build tooling.

## Status

Version `0000` (initial scaffolding).
